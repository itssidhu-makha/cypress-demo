pipeline {
  agent {
    // this image provides everything needed to run Cypress
    docker {
      image 'cypress/base:12'
       args '-u root:root'
    }
  }

  stages {
    stage('build and test') {
      

      steps {
        sh 'npm ci'
        sh "npm test"
      }
    }
  }
}